import { beforeEach, afterEach, vi } from "vitest";

// This file runs before all tests.
// We can define global mocks or common setup here.
beforeEach(() => {
  // Common setup for tests
});

afterEach(() => {
  // Common cleanup, like resetting mocks
  vi.clearAllMocks();
});
