## 1. Setup

- [x] 1.1 Create the `.github/` directory if it doesn't already exist.
- [x] 1.2 Create the `.github/dependabot.yml` file.

## 2. Configuration

- [x] 2.1 Add the version and core configuration to `dependabot.yml`.
- [x] 2.2 Configure the `npm` ecosystem for daily updates in the root directory.
- [x] 2.3 Configure the `github-actions` ecosystem for daily updates in the root directory.

## 3. Verification

- [x] 3.1 Verify the Dependabot YAML syntax and structure.
- [x] 3.2 Ensure the configuration covers both package and action updates.
